-- 帳號頭像
--
-- 圖檔本身放檔案系統（容器掛的 /data/attachments），資料庫只記檔名。
-- 不把圖塞進資料庫的理由：備份、複寫、連線池都會被幾百 KB 的二進位資料拖累，
-- 而頭像是「壞掉也只是變回文字縮寫」的東西，不值得那個代價。
--
-- 存檔名而不是完整路徑：搬機器、改掛載點的時候不用改資料。

ALTER TABLE app_user ADD COLUMN avatar_file text;
