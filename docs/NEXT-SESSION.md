# 下次從這裡繼續

2026-08-05 收工。上一批（`f55dca4` wip 檢查點）已經收尾、驗過並推上 main。

---

## 貼給 Claude 的開場白（直接複製）

```
繼續 PMFlow。先讀 D:\NewProject\CLAUDE.md（共用規矩）與 docs/NEXT-SESSION.md，
再看 docs/CHANGELOG.md 最上面的索引。照「還沒動工」的順序做，
不衝突就開多個子代理，但要先分好誰改哪些檔。
```

---

## 2026-08-05 這一輪做完並驗過的

- **深色模式與文案外移收尾**：剩下的七、八個畫面全部收完
- **深色的次要文字看不清楚**：配色對照表本身訂錯了（`dark:text-slate-500` 只有
  3～4.2:1），整條往上提一階，量到的數字寫進 `index.css`。
  改前整頁 140 處不合格，改後剩 1 處
- **請假**：路由掛上、行事曆畫出請假長條、本人與管理者可填
- **專案設公開**：專案卡片上的開關
- **按不動的按鈕不畫出來**：任務詳情、清單、看板
- **任意帳號都能開專案，開的人就是專案管理者**；管理者可以搜尋任意帳號拉人進來
- **發文追蹤移進專案**（頁籤的最後一個），**成員移進頭像選單**

## 還沒動工（他交辦的順序）

1. **每個專案自己的「系統參數」頁**：下拉清單（任務狀態、優先度、任務類型）的值與
   順序可以改。**是每個專案改自己的**，不是全站一份，權限給專案管理者。
2. **任務轉派**：畫面上還是沒有指派負責人的介面（後端 `PATCH /tasks/:id` 早就支援）。
   先補指派，再做「轉派時附一句交接說明、寫進活動紀錄」。

## 已知的 bug（還沒修）

**上傳的頭像顯示不出來**：`GET /users/:id/avatar` 要 `Authorization` 標頭，
而 `<img src>` 不會帶 → 401 → 退回文字色塊。
建議前端改成 fetch 拿 blob URL，不要放寬那個端點的驗證。

## M4（視覺化）剩下的

- 儀表板圖表：燃盡圖與負載熱圖的端點還沒查證過
- 逾期提醒信 —— 但**系統目前沒有寄信的能力**，要先決定是不是真的要做

## 別忘了

- 規矩全在 `D:\NewProject\CLAUDE.md`，那個檔每個視窗都會自動讀到，
  新的規則寫進去、不要另外開檔
- 每批改動寫進 `docs/CHANGELOG.md`（索引一列＋詳細條目一段）
- 發版的 bat 交出去就停手，等他回覆
